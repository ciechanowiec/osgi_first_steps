/**
 * The example 8 was supposed to serve as a demonstration of Service
 * Binder (org.apache.felix.servicebinder) functionalities. However,
 * the Service Binder has been removed from the Apache Felix, so
 * it wasn't implemented here
 */
